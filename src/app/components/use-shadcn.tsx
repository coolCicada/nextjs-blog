'use client';
import { Button } from "@/components/ui/button"

export default function UseShadcn() {
    return (
        <div>
            <Button onClick={() => {
                console.log('123')
            }}>123</Button>
        </div>
    )
}